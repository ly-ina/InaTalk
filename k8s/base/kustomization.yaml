apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ruanks

resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: ruanks
  app.kubernetes.io/part-of: light-chat

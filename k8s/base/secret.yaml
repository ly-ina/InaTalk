# ============================================
# 敏感配置：Supabase 凭证
# ⚠️ 部署前必须替换为真实值！
#    生产环境建议用 kubectl create secret 或外挂 vault
# ============================================
apiVersion: v1
kind: Secret
metadata:
  name: ruanks-secret
  namespace: ruanks
type: Opaque
stringData:
  SUPABASE_URL: "https://xxxxxxxxxxxx.supabase.co"
  SUPABASE_ANON_KEY: "your-anon-key-here"
